# M2-B2 — requirements
# Python 3.11+

# Core data
pandas==2.2.2
numpy==1.26.4

# Génération du dataset (commentaires synthétiques PII, scripts/generate_dataset.py)
faker==25.9.1
# certifi : magasin de certificats pour le téléchargement UCI via urllib
# (évite CERTIFICATE_VERIFY_FAILED sur un venv frais macOS/Windows)
certifi==2024.8.30
# scikit-learn : fallback OpenML si UCI est injoignable (fetch_openml)
scikit-learn==1.5.1

# Visualisation
matplotlib==3.9.2
seaborn==0.13.2

# NLP — détection PII
spacy==3.7.5
# click requis explicitement : depuis typer 0.13+, `typer-slim` n'embarque
# plus click, et spacy 3.7.5 (via sa CLI) en a besoin pour s'importer. Sans ce
# pin, un install frais casse sur `ModuleNotFoundError: No module named 'click'`.
click==8.1.7

# Persistance
pyarrow==17.0.0

# Notebook
jupyter==1.1.1
ipykernel==6.29.5

# Optionnel — décommente seulement si tu pousses sur la stratégie Presidio
# presidio-analyzer==2.2.355
# presidio-anonymizer==2.2.355